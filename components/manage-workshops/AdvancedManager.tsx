import { Grid } from '@mui/material';

const AdvancedManager = (): JSX.Element => {
  return (
    <Grid container p={8} justifyContent="center">
      Advanced manger{' '}
    </Grid>
  );
};

export default AdvancedManager;
